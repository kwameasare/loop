name: helm-e2e

on:
  pull_request:
    paths:
      - ".github/workflows/helm-e2e.yml"
      - "infra/helm/loop/**"
      - "scripts/helm_e2e_smoke_server.py"
  push:
    branches: [main]
    paths:
      - ".github/workflows/helm-e2e.yml"
      - "infra/helm/loop/**"
      - "scripts/helm_e2e_smoke_server.py"
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * *"

jobs:
  helm-e2e:
    name: helm-e2e
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - uses: helm/kind-action@v1.10.0
        with:
          cluster_name: loop-helm-e2e
          wait: 60s
      - name: Build and load smoke image
        run: |
          cat > /tmp/loop-helm-smoke.Dockerfile <<'DOCKER'
          FROM python:3.12-alpine
          COPY scripts/helm_e2e_smoke_server.py /server.py
          ENV HELM_SMOKE_PORTS=8080,8081,8082,8003
          CMD ["python", "/server.py"]
          DOCKER
          docker build -f /tmp/loop-helm-smoke.Dockerfile -t loop/helm-smoke:ci .
          kind load docker-image loop/helm-smoke:ci --name loop-helm-e2e
      - name: Install Loop chart
        run: |
          helm dependency build infra/helm/loop
          helm upgrade --install loop infra/helm/loop \
            --wait --timeout 8m \
            --set global.imageRegistry=loop \
            --set global.imagePullPolicy=IfNotPresent \
            --set controlPlane.image.repository=helm-smoke \
            --set controlPlane.image.tag=ci \
            --set controlPlane.replicaCount=1 \
            --set runtime.image.repository=helm-smoke \
            --set runtime.image.tag=ci \
            --set runtime.replicaCount=1 \
            --set gateway.image.repository=helm-smoke \
            --set gateway.image.tag=ci \
            --set gateway.replicaCount=1 \
            --set kbEngine.image.repository=helm-smoke \
            --set kbEngine.image.tag=ci \
            --set kbEngine.replicaCount=1 \
            --set toolHost.enabled=false \
            --set postgresql.enabled=false \
            --set redis.enabled=false \
            --set qdrant.enabled=false \
            --set nats.enabled=false \
            --set minio.enabled=false \
            --set clickhouse.enabled=false
      - name: Send a turn through dp-runtime
        run: |
          kubectl rollout status deploy/loop-loop-runtime --timeout=90s
          kubectl port-forward svc/loop-loop-runtime 18081:8081 >/tmp/loop-runtime.log 2>&1 &
          pf_pid=$!
          trap 'kill "$pf_pid"' EXIT
          for _ in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:18081/healthz >/dev/null; then
              break
            fi
            sleep 1
          done
          curl -fsS -X POST http://127.0.0.1:18081/v1/turns \
            -H 'content-type: application/json' \
            -d '{"input":"hello from helm e2e"}' | tee /tmp/turn.json
          grep -q "helm-e2e-ok" /tmp/turn.json
      - name: Dump cluster diagnostics
        if: failure()
        run: |
          kubectl get all
          kubectl describe pods
          kubectl logs -l app.kubernetes.io/instance=loop --all-containers --tail=120
