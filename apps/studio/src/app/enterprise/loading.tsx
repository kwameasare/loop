import { SectionLoading } from "@/components/section-states";

export default function EnterpriseLoading() {
  return (
    <SectionLoading title="Enterprise" subtitle="Loading SSO configuration…" />
  );
}
