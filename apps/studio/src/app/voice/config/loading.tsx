import { SectionLoading } from "@/components/section-states";

export default function VoiceConfigLoading() {
  return (
    <SectionLoading
      title="Voice channel configuration"
      subtitle="Loading numbers + providers…"
    />
  );
}
