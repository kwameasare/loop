[project]
name = "loop-data-plane"
version = "0.1.0"
description = "Loop data-plane: conversations, turns, memory. Per-region Postgres migrations."
license = { text = "Apache-2.0" }
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.13",
    "fastapi>=0.115",
    "loop",
    "loop-gateway",
    "loop-runtime",
    # OpenTelemetry tracing (P0.7c). Auto-instruments FastAPI so every
    # request becomes a span propagated to gateway via traceparent.
    "opentelemetry-api>=1.27",
    "opentelemetry-sdk>=1.27",
    "opentelemetry-instrumentation-fastapi>=0.48b0",
    "opentelemetry-exporter-otlp-proto-http>=1.27",
    # Prometheus middleware (P0.7b). The `slo-burn.yaml` dp-runtime
    # SLOs target the series this client emits.
    "prometheus-client>=0.20",
    "pydantic>=2",
    "sqlalchemy>=2.0",
    "uvicorn>=0.30",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["loop_data_plane"]
