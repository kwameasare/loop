{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [
        {"expr": "sum(rate(http_server_requests_total[5m]))", "legendFormat": "req/s"}
      ],
      "title": "Platform Request Rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "percentunit"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [
        {
          "expr": "1 - (sum(rate(http_server_requests_total{status=~\"5..\"}[5m])) / sum(rate(http_server_requests_total[5m])))",
          "legendFormat": "availability"
        }
      ],
      "title": "Platform Availability",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 38,
  "tags": ["loop", "overview"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "Loop Platform Overview",
  "version": 1
}
